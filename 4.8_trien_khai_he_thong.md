# 4.8. Triển khai hệ thống

Sau khi hoàn thành quá trình xây dựng và kiểm thử các chức năng chính, hệ thống cần được chuẩn bị để có thể chạy trong môi trường triển khai thực tế. Việc triển khai hệ thống không chỉ đơn thuần là đưa mã nguồn lên máy chủ, mà còn bao gồm quá trình cấu hình môi trường, cài đặt phụ thuộc, kết nối cơ sở dữ liệu, thiết lập biến môi trường, kiểm tra API, cấu hình đường dẫn frontend và bảo đảm các dịch vụ hỗ trợ như email, upload, thông báo thời gian thực và tư vấn AI hoạt động đúng.

Trong phạm vi đồ án, hệ thống được tổ chức theo mô hình tách riêng frontend và backend. Frontend được xây dựng bằng ReactJS/Vite và biên dịch thành các tệp tĩnh để phục vụ người dùng trên trình duyệt. Backend được xây dựng bằng Node.js/Express, chịu trách nhiệm xử lý API, xác thực, phân quyền, nghiệp vụ lịch hẹn, hồ sơ khám bệnh, thông báo, xuất PDF và kết nối cơ sở dữ liệu MongoDB. Cách tổ chức này phù hợp với mô hình client - server đã trình bày ở các phần trước, đồng thời tạo điều kiện thuận lợi cho việc triển khai, bảo trì và mở rộng hệ thống.

## 4.8.1. Mô hình triển khai hệ thống

Mô hình triển khai của hệ thống gồm ba thành phần chính: ứng dụng frontend, dịch vụ backend API và cơ sở dữ liệu MongoDB. Người dùng truy cập hệ thống thông qua trình duyệt web. Trình duyệt tải giao diện ReactJS từ dịch vụ frontend, sau đó gửi các request đến backend thông qua địa chỉ API được cấu hình trong biến môi trường `VITE_API_URL`. Backend tiếp nhận request, kiểm tra xác thực và phân quyền, xử lý nghiệp vụ, truy vấn hoặc cập nhật dữ liệu trong MongoDB rồi trả kết quả về frontend.

Đối với môi trường triển khai production, dự án đã chuẩn bị cấu hình theo hướng sử dụng Render. Trong đó, backend được triển khai dưới dạng Node web service, còn frontend được triển khai dưới dạng static web service. Backend có endpoint kiểm tra trạng thái `GET /health` để nền tảng triển khai có thể xác nhận dịch vụ đang hoạt động. Frontend sử dụng cơ chế rewrite tất cả route về `index.html` để React Router có thể xử lý điều hướng phía client.

Sơ đồ triển khai có thể mô tả ở mức tổng quát như sau:

Tên ảnh đề xuất: `Hinh_4_49_mo_hinh_trien_khai_he_thong.png`

Trong mô hình này, frontend và backend được tách riêng về mặt triển khai nhưng phối hợp với nhau thông qua RESTful API và Socket.IO. MongoDB đóng vai trò lưu trữ dữ liệu trung tâm, bao gồm thông tin tài khoản, bác sĩ, chuyên khoa, cơ sở khám, lịch hẹn, hồ sơ khám bệnh, thông báo, đánh giá và nhật ký hệ thống. Các dịch vụ bên ngoài như SMTP/Brevo và Gemini API được kết nối thông qua backend để bảo vệ thông tin cấu hình nhạy cảm.

## 4.8.2. Chuẩn bị mã nguồn và môi trường triển khai

Trước khi triển khai, mã nguồn cần được kiểm tra lại cấu trúc thư mục, các file cấu hình và các script chạy ứng dụng. Dự án được chia thành hai thư mục chính là `backend` và `frontend`. Thư mục `backend` chứa mã nguồn Express API, model Mongoose, controller, middleware, service, route và các script seed dữ liệu. Thư mục `frontend` chứa mã nguồn ReactJS, các trang giao diện, component, context, service kết nối socket, API client và file CSS giao diện.

Các yêu cầu môi trường cơ bản của dự án gồm Node.js, npm, MongoDB và Git. Ở môi trường phát triển cục bộ, backend có thể chạy trên cổng 5000 và frontend chạy trên cổng 5173. Ở môi trường production, cổng của backend được nền tảng triển khai cấp qua biến môi trường `PORT`, trong khi frontend được build thành thư mục `dist` và phục vụ như một static site.

Các lệnh chuẩn bị và chạy ứng dụng ở môi trường cục bộ gồm:

| Thành phần | Lệnh thực hiện | Mục đích |
| --- | --- | --- |
| Backend | `cd backend` | Chuyển vào thư mục backend |
| Backend | `npm install` hoặc `npm ci` | Cài đặt phụ thuộc backend |
| Backend | `npm run seed` | Khởi tạo dữ liệu nền nếu cần |
| Backend | `npm run seed:sample` | Tạo dữ liệu mẫu phục vụ demo |
| Backend | `npm run dev` | Chạy backend ở môi trường phát triển |
| Frontend | `cd frontend` | Chuyển vào thư mục frontend |
| Frontend | `npm install` hoặc `npm ci` | Cài đặt phụ thuộc frontend |
| Frontend | `npm run dev` | Chạy frontend ở môi trường phát triển |
| Frontend | `npm run build` | Build frontend thành bản production |

Trước khi đưa lên production, cần bảo đảm mã nguồn đã được kiểm thử, các dữ liệu mẫu không bị đưa vào môi trường thật một cách tùy tiện và các thông tin nhạy cảm không được lưu trực tiếp trong mã nguồn. Những thông tin như chuỗi kết nối MongoDB, khóa JWT, tài khoản SMTP, khóa Brevo và khóa Gemini API phải được khai báo bằng biến môi trường trên nền tảng triển khai.

## 4.8.3. Triển khai backend, frontend và cơ sở dữ liệu

Backend được triển khai như một dịch vụ Node.js. File cấu hình triển khai xác định thư mục gốc của backend là `backend`, lệnh build là `npm ci` và lệnh chạy là `npm start`. Khi dịch vụ khởi động, backend thực hiện kết nối cơ sở dữ liệu, đồng bộ một số index quan trọng của các collection như lịch hẹn, danh sách chờ và outbox đồng bộ cơ sở, sau đó tạo HTTP server và khởi tạo Socket.IO để phục vụ thông báo thời gian thực.

Backend cũng khởi động một số tác vụ nền phục vụ nghiệp vụ vận hành như kiểm tra lời mời danh sách chờ hết hạn, cập nhật trạng thái bệnh nhân không đến khám, theo dõi lịch tái khám và đồng bộ dữ liệu cơ sở. Các tác vụ này giúp hệ thống không chỉ phản hồi request trực tiếp từ người dùng mà còn có thể xử lý các sự kiện định kỳ trong quá trình vận hành.

Frontend được triển khai dưới dạng static site. Quá trình build frontend sử dụng lệnh `npm ci && npm run build`, tạo ra thư mục `dist`. Các file trong thư mục này được phục vụ trực tiếp cho trình duyệt. Vì ứng dụng sử dụng React Router, cấu hình triển khai cần rewrite mọi route về `index.html`. Cách cấu hình này giúp người dùng có thể truy cập trực tiếp các đường dẫn như trang lịch hẹn, hồ sơ khám hoặc trang quản trị mà không bị lỗi 404 từ static server.

Cơ sở dữ liệu MongoDB có thể triển khai theo hai hướng. Ở môi trường cục bộ, hệ thống sử dụng MongoDB chạy trên máy phát triển. Ở môi trường production, nên sử dụng MongoDB Atlas hoặc một dịch vụ MongoDB cloud để bảo đảm khả năng truy cập ổn định, sao lưu dữ liệu và quản lý bảo mật tốt hơn. Dự án có hỗ trợ cấu hình `MONGO_URI`, `CENTRAL_MONGO_URI` và `CLINIC_DB_PREFIX`, phù hợp với hướng tổ chức dữ liệu trung tâm và dữ liệu theo cơ sở/chi nhánh.

## 4.8.4. Cấu hình biến môi trường và dịch vụ hỗ trợ

Biến môi trường là thành phần quan trọng trong quá trình triển khai vì hệ thống có nhiều thông tin cấu hình khác nhau giữa môi trường phát triển và môi trường production. Các biến này không nên ghi cứng trong mã nguồn để tránh rò rỉ thông tin nhạy cảm và giúp hệ thống dễ thay đổi cấu hình khi chuyển môi trường.

Bảng dưới đây trình bày các nhóm biến môi trường chính của hệ thống:

| Nhóm cấu hình | Biến môi trường | Ý nghĩa |
| --- | --- | --- |
| Server | `NODE_ENV`, `PORT` | Xác định môi trường chạy và cổng backend |
| Database | `MONGO_URI`, `CENTRAL_MONGO_URI`, `CLINIC_DB_PREFIX` | Kết nối MongoDB và quy ước database theo cơ sở |
| Xác thực | `JWT_SECRET`, `JWT_EXPIRES_IN` | Tạo và kiểm tra JWT |
| Frontend URL | `APP_URL` | Địa chỉ frontend dùng trong email, liên kết và CORS nghiệp vụ |
| Frontend API | `VITE_API_URL` | Địa chỉ API backend để frontend gọi request |
| Email SMTP | `SMTP_HOST`, `SMTP_PORT`, `SMTP_SECURE`, `SMTP_USER`, `SMTP_PASS`, `EMAIL_FROM` | Gửi email OTP, xác nhận lịch và thông báo |
| Email Brevo | `BREVO_API_KEY`, `BREVO_SENDER_EMAIL`, `BREVO_SENDER_NAME`, `BREVO_REPLY_TO` | Gửi email qua dịch vụ Brevo nếu được cấu hình |
| AI | `GEMINI_API_KEY`, `GEMINI_MODEL` | Kết nối dịch vụ tư vấn triệu chứng bằng AI |

Đối với backend, cần cấu hình JWT đủ mạnh trong production để bảo vệ phiên đăng nhập. Đối với MongoDB, cần sử dụng chuỗi kết nối có tài khoản và mật khẩu riêng, giới hạn IP truy cập nếu dùng MongoDB Atlas. Đối với email, nếu chưa cấu hình SMTP hoặc Brevo, hệ thống vẫn có thể xử lý nghiệp vụ chính nhưng các email OTP, xác nhận lịch hoặc thông báo qua email sẽ không được gửi đầy đủ.

Đối với upload file, backend hiện phục vụ tệp tĩnh qua đường dẫn `/uploads`. Cách này phù hợp với môi trường phát triển và demo cục bộ. Tuy nhiên, khi triển khai production trên các nền tảng có filesystem không bền vững hoặc có thể bị xóa sau mỗi lần redeploy, nên chuyển các tệp upload sang dịch vụ lưu trữ bên ngoài như Cloudinary, AWS S3 hoặc một dịch vụ object storage tương đương. Đây là điểm cần lưu ý nếu hệ thống được vận hành lâu dài.

## 4.8.5. Kết quả triển khai thực tế

Qua rà soát mã nguồn, dự án đã có sẵn cấu hình triển khai cho hai dịch vụ chính. Dịch vụ backend được khai báo với tên `clinic-booking-backend`, runtime Node.js, thư mục gốc `backend`, lệnh build `npm ci`, lệnh chạy `npm start` và health check tại `/health`. Dịch vụ frontend được khai báo với tên `clinic-booking`, runtime static, thư mục gốc `frontend`, lệnh build `npm ci && npm run build` và thư mục xuất bản `dist`.

Khi triển khai thành công, hệ thống sẽ có hai địa chỉ public: một địa chỉ cho frontend để người dùng truy cập giao diện và một địa chỉ cho backend để frontend gọi API. Biến `VITE_API_URL` của frontend cần trỏ đến địa chỉ backend kèm tiền tố `/api`. Biến `APP_URL` của backend cần trỏ về địa chỉ frontend để các liên kết trong email hoặc thông báo điều hướng đúng màn hình.

Các kết quả có thể kiểm tra sau khi triển khai gồm:

| Nội dung kiểm tra | Cách kiểm tra | Kết quả mong đợi |
| --- | --- | --- |
| Backend khởi động | Truy cập `GET /health` | Trả về trạng thái `ok` |
| Frontend hoạt động | Truy cập URL frontend | Trang chủ hiển thị bình thường |
| Kết nối API | Đăng nhập hoặc tải danh sách bác sĩ | Frontend nhận dữ liệu từ backend |
| Kết nối MongoDB | Tạo hoặc đọc lịch hẹn | Dữ liệu được lưu và truy vấn thành công |
| Xác thực | Đăng nhập bằng tài khoản mẫu | Nhận JWT và điều hướng đúng vai trò |
| Upload | Tải ảnh hoặc tệp hồ sơ | Tệp được lưu và trả về URL |
| PDF | Tải phiếu đặt lịch hoặc phiếu kết quả khám | Trình duyệt tải file PDF hợp lệ |
| Thông báo | Tạo sự kiện đặt lịch hoặc cập nhật hồ sơ | Thông báo được lưu và hiển thị |
| AI | Gửi triệu chứng đến trang tư vấn | Hệ thống trả phân tích hoặc fallback |

Trong phạm vi đồ án, cấu hình triển khai đã đủ để đưa hệ thống lên môi trường cloud phục vụ demo và đánh giá chức năng. Tuy nhiên, trước khi vận hành chính thức cho người dùng thật, cần kiểm tra lại domain, HTTPS, bảo mật biến môi trường, cơ chế sao lưu dữ liệu, cấu hình lưu trữ file upload và giới hạn truy cập database.

## 4.8.6. Hạn chế của môi trường triển khai

Mặc dù hệ thống đã có cấu hình triển khai tương đối đầy đủ, môi trường triển khai trong phạm vi đồ án vẫn còn một số hạn chế. Thứ nhất, nếu sử dụng gói miễn phí của nền tảng cloud, backend có thể bị sleep sau một thời gian không có request, dẫn đến lần truy cập đầu tiên chậm hơn. Điều này không ảnh hưởng nhiều đến demo chức năng nhưng chưa phù hợp với hệ thống y tế cần phản hồi ổn định liên tục.

Thứ hai, cơ chế lưu trữ file upload bằng thư mục cục bộ `/uploads` chưa thật sự phù hợp với production lâu dài. Khi dịch vụ được redeploy hoặc chạy trên môi trường container không có ổ đĩa bền vững, các tệp đã upload có thể không được giữ lại. Vì vậy, cần chuyển sang giải pháp lưu trữ file chuyên dụng nếu triển khai thật.

Thứ ba, các dịch vụ bên ngoài như SMTP/Brevo và Gemini API phụ thuộc vào khóa cấu hình, quota và kết nối mạng. Nếu các dịch vụ này không khả dụng, hệ thống cần sử dụng cơ chế fallback để vẫn duy trì nghiệp vụ chính. Trong mã nguồn, chức năng tư vấn AI đã có hướng fallback dựa trên từ khóa khi API AI không sử dụng được; tuy nhiên, các chức năng email vẫn cần cấu hình đầy đủ để bảo đảm người dùng nhận được OTP và thông báo.

Thứ tư, hệ thống chưa có pipeline CI/CD hoàn chỉnh để tự động chạy kiểm thử, build và triển khai sau mỗi lần thay đổi mã nguồn. Việc triển khai hiện chủ yếu dựa trên cấu hình dịch vụ và thao tác cập nhật mã nguồn. Trong tương lai, có thể bổ sung GitHub Actions hoặc quy trình CI/CD tương tự để tự động chạy smoke test, kiểm tra build frontend, kiểm tra backend và chỉ triển khai khi các bước này thành công.

Nhìn chung, môi trường triển khai hiện tại phù hợp với mục tiêu demo đồ án, kiểm chứng tính khả thi của kiến trúc và trình bày kết quả xây dựng ứng dụng. Để đưa hệ thống vào vận hành thực tế, cần tiếp tục hoàn thiện hạ tầng production theo hướng ổn định hơn, bao gồm lưu trữ file bền vững, backup database định kỳ, giám sát log, cảnh báo lỗi, HTTPS, giới hạn truy cập quản trị và quy trình triển khai tự động.
